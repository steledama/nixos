# ==========================================
# CONFIGURAZIONE INFRASTRUTTURA DOCKER
# ==========================================
# Questo file contiene le variabili di ambiente per i container Docker.
#
# SETUP INIZIALE:
# 1. Copia questo file in .env: cp .env.example .env
# 2. Modifica .env con le tue credenziali reali
# 3. NON committare mai il file .env (è in .gitignore)
#
# USATO DA: compose.*.yml (Baserow, WordPress, Nginx)
# ==========================================

# ==========================================
# BASEROW - Low-Code Database Platform
# ==========================================
# URL pubblico per accedere a Baserow (usato per link email e API)
# Default: http://IP_SERVER:8385
BASEROW_PUBLIC_URL=http://5.89.62.125:8385

# ==========================================
# EMAIL SMTP - Configurazione invio email
# ==========================================
# Email mittente per notifiche sistema (Baserow, WordPress)
FROM_EMAIL=sviluppo@toscanatradingsrl.com

# Abilita invio email via SMTP (true/false)
EMAIL_SMTP=true

# Server SMTP per invio email
# Esempio: mail.tuodominio.com, smtp.gmail.com
EMAIL_SMTP_HOST=mail.toscanatradingsrl.com

# Porta SMTP
# 25 (non criptato), 465 (SSL/TLS), 587 (STARTTLS)
EMAIL_SMTP_PORT=465

# Usa connessione SSL/TLS (true per porta 465, false per 587)
EMAIL_SMTP_USE_SSL=true

# Credenziali account email SMTP
EMAIL_SMTP_USER=sviluppo@toscanatradingsrl.com
EMAIL_SMTP_PASSWORD=tua_password_email_qui

# ==========================================
# DOCKER - Configurazione generale container
# ==========================================
# UID e GID per permessi file WordPress
# Ottieni con: id -u nome_utente && id -g nome_utente
# Importante: devono corrispondere all'utente che gestisce i file
WP_UID=1002
WP_GID=100

# ==========================================
# WORDPRESS - Configurazione debug
# ==========================================
# Abilita modalità debug WordPress (true/false)
# ATTENZIONE: Usa false in produzione per performance e sicurezza
WP_DEBUG=false

# Salva log di debug in wp-content/debug.log (true/false)
WP_DEBUG_LOG=true

# Mostra errori nel browser (true/false)
# ATTENZIONE: Imposta false in produzione per non esporre informazioni sensibili
WP_DEBUG_DISPLAY=false

# ==========================================
# NGINX PROXY - Configurazione reverse proxy
# ==========================================
# Hostname o IP del server per configurazione nginx
# Usato per generare configurazioni SSL e virtual host
SERVER_HOSTNAME=5.89.62.125

# ==========================================
# PORTE SERVIZI - Esposizione container
# ==========================================
# Porta HTTPS per Toscana Trading (default: 8443)
# Accessibile su: https://SERVER_HOSTNAME:8443
TOSCANA_HTTPS_PORT=8443

# Porta HTTPS per Flexora (default: 8444)
# Accessibile su: https://SERVER_HOSTNAME:8444
FLEXORA_HTTPS_PORT=8444

# ==========================================
# DATABASE TOSCANA TRADING
# ==========================================
# Nome database WordPress
TOSCANA_DB_NAME=toscana_wp

# Utente database (non root, per sicurezza)
TOSCANA_DB_USER=toscana_user

# Password utente database
# IMPORTANTE: Usa password complessa (mix lettere, numeri, simboli)
TOSCANA_DB_PASSWORD=genera_password_sicura_qui

# Password root database
# IMPORTANTE: Password diversa da quella utente, ancora più complessa
TOSCANA_DB_ROOT_PASSWORD=genera_password_root_sicura_qui

# Host database (nome servizio Docker, NON modificare)
TOSCANA_DB_HOST=toscana-db

# Prefisso tabelle WordPress (personalizzabile)
# Default WordPress: wp_ - consigliato cambiarlo per sicurezza
TOSCANA_TABLE_PREFIX=tt_

# ==========================================
# DATABASE FLEXORA
# ==========================================
# Nome database WordPress
FLEXORA_DB_NAME=flexora_wp

# Utente database (non root, per sicurezza)
FLEXORA_DB_USER=flexora_user

# Password utente database
# IMPORTANTE: Usa password complessa e DIVERSA da Toscana
FLEXORA_DB_PASSWORD=genera_password_sicura_qui

# Password root database
# IMPORTANTE: Password diversa da quella utente e da Toscana root
FLEXORA_DB_ROOT_PASSWORD=genera_password_root_sicura_qui

# Host database (nome servizio Docker, NON modificare)
FLEXORA_DB_HOST=flexora-db

# Prefisso tabelle WordPress (personalizzabile)
# Default WordPress: wp_ - consigliato cambiarlo per sicurezza
FLEXORA_TABLE_PREFIX=fx_

# ==========================================
# NOTE DI SICUREZZA
# ==========================================
# 1. Genera password sicure con: openssl rand -base64 32
# 2. Usa password diverse per ogni servizio
# 3. Cambia password periodicamente
# 4. Backup regolare dei database
# 5. Non condividere mai questo file con credenziali reali
#
# BACKUP DATABASE:
# docker exec toscana-db mysqldump -u root -p toscana_wp > backup.sql
# docker exec flexora-db mysqldump -u root -p flexora_wp > backup.sql
