# ==========================================
# BASEROW CONFIGURATION
# ==========================================
# This file contains environment variables for Baserow container.
#
# INITIAL SETUP:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit .env with your real credentials
# 3. NEVER commit the .env file (it's in .gitignore)
#
# USED BY: baserow.yml
# ==========================================

# ==========================================
# BASEROW - Low-Code Database Platform
# ==========================================
# Public URL to access Baserow (used for email links and API)
# Example: http://YOUR_SERVER_IP:8385
BASEROW_PUBLIC_URL=http://YOUR_SERVER_IP:8385

# ==========================================
# EMAIL SMTP - Email sending configuration
# ==========================================
# Sender email for system notifications
# Example: noreply@yourdomain.com
FROM_EMAIL=noreply@yourdomain.com

# Enable email sending via SMTP (true/false)
EMAIL_SMTP=true

# SMTP server for sending emails
# Examples: mail.yourdomain.com, smtp.gmail.com, smtp.sendgrid.net
EMAIL_SMTP_HOST=mail.yourdomain.com

# SMTP port
# 25 (unencrypted), 465 (SSL/TLS), 587 (STARTTLS)
EMAIL_SMTP_PORT=465

# Use SSL/TLS connection (true for port 465, false for port 587)
EMAIL_SMTP_USE_SSL=true

# SMTP account credentials
EMAIL_SMTP_USER=your_email@yourdomain.com
EMAIL_SMTP_PASSWORD=your_smtp_password_here

# ==========================================
# SECURITY NOTES
# ==========================================
# 1. Generate secure passwords with: openssl rand -base64 32
# 2. Never share this file with real credentials
# 3. Regular backups recommended (use Baserow GUI: Workspace → Export data)
#
# BASEROW BACKUP (GUI method - recommended):
# 1. Login to Baserow → http://YOUR_SERVER_IP:8385
# 2. Select workspace → Menu → 'Export data'
# 3. Download compact ZIP (~7MB)
#

# ==========================================
# NGINX REVERSE PROXY
# ==========================================
# Server hostname/IP for SSL certificates and routing
SERVER_HOSTNAME=YOUR_SERVER_IP

# HTTPS ports for WordPress sites
TOSCANA_HTTPS_PORT=8443
FLEXORA_HTTPS_PORT=8444
